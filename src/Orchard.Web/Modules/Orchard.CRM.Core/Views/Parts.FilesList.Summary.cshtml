<span class="files-count">
    @if (Model.Count == 0)
    {
        @T("No files")
    }
    else
    {
        int count = Model.Count;
        <span>@count</span>
        <span>&nbsp;@T("files")</span>
    }
</span>