@using Orchard.Comments.Models
@{
    CommentPart comment = Model.ContentPart;
}

<div class="comment-author"><i class="icon-user"></i>&nbsp;<a href="#">@Html.LinkOrDefault(comment.Author, comment.SiteName, new { rel = "nofollow" })</a></div>